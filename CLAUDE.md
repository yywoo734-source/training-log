# 훈련기록 (운동앱)

개인 1인용 운동·식단·회복 기록 앱. 단순 기록이 아니라 **다음 행동을 추천**하는 것이 목적.

## 스택 — 왜 이걸 골랐는지

| 항목 | 선택 | 이유 |
|---|---|---|
| 형태 | 웹앱(PWA) | 앱스토어 심사·인증서 없이 오늘 만들어 오늘 헬스장에서 씀. 홈화면 추가 시 앱처럼 뜸 |
| 저장 | localStorage 한 덩어리 | 1인용이라 서버·로그인 불필요. 1년치 기록해도 수백 KB |
| 상태관리 | `useSyncExternalStore` (store.ts) | Redux/Zustand 불필요. 구독 30줄로 끝 |
| 라우팅 | `useState` 탭 | 탭 5개짜리에 react-router는 과함 |
| 스타일 | 단일 CSS 파일 + 토큰 | Tailwind 설정 비용보다 직접 쓰는 게 짧음 |
| 오프라인 | vite-plugin-pwa | 헬스장 지하에서 신호 없어도 열려야 함 |

**의존성은 react / react-dom + 빌드 도구가 전부다. 새 라이브러리를 넣기 전에 이 표를 먼저 볼 것.**

## 원래 기획서와 달라진 곳 (중요)

기획서를 그대로 구현하지 않은 지점들. 되돌리기 전에 이유를 읽을 것.

1. **`WorkoutSet`을 별도 테이블로 두지 않음**
   기획서는 `WorkoutSet.workoutSessionId`로 연결하는 서버 DB 스키마였다. 저장소가 폰 안 JSON이라 조인할 대상이 없다. `Session.entries[].sets[]`로 품었다.

2. **`Phase`(감량기/유지기/증량기)를 새로 넣음** — 기획서에 없던 것
   없으면 추천 로직이 모순된다. 감량 중에는 중량이 안 오르는 게 정상인데, 기획서의 "2회 연속 하한 미달 → 감량" 규칙이 그걸 실패로 읽고 무게를 계속 깎는다. 감량기에는 한 세션 더 기다리도록 했다 (`progression.ts`의 `strikesNeeded`).

3. **`Exercise.loadType`을 새로 넣음** — 기획서에 없던 것
   어시스트 풀업은 **보조중량이 낮을수록 강한 것**이다. 다른 종목과 같은 로직에 넣으면 "잘했으니 보조를 올리자"고 거꾸로 추천한다. `loadType: 'assist'`면 방향이 뒤집힌다.

4. **걸음수 타일 제거**
   웹앱은 애플 건강 데이터를 못 읽는다. 손으로 입력하는 걸음수는 아무도 안 쓴다. 네이티브로 옮길 때 부활시킬 것.

5. **식단은 프리셋 탭 방식**
   매 끼니 칼로리·탄단지를 손으로 넣는 방식은 며칠 못 간다. 자주 먹는 음식을 등록해두고 탭 한 번으로 기록한다. 같은 끼니에 이미 있으면 수량만 올라간다.

6. **매일 회복 기록은 4개만 필수**
   수면·수면만족도·피로·근육통. 통증/스트레스/동기는 "더 기록하기"를 눌러야 나온다. 13개를 매일 받으면 2주 만에 멈춘다.

7. **요일 고정이 아니라 "그 날의 세션"**
   클라이밍 날짜가 매주 바뀌므로 요일은 기본 제안일 뿐이다. 운동 화면 우상단 드롭다운으로 그 날의 세션을 바꾼다 (`plannedRoutineId`).

## 회복 점수 가중치는 튜닝 값이다

`recovery.ts`의 `WEIGHTS`는 근거 있는 수치가 아니라 시작점이다. 수면 30 / 수면만족 15 / 피로 20 / 근육통 15 / 통증 10 / 전일부하 10 = 100.
몇 주 써보고 체감과 안 맞으면 **여기만** 고친다. 로직은 건드리지 말 것.

## 작업 규칙

- 로직을 고치면 `src/lib/logic.test.ts`에 케이스를 추가한다. 특히 증량 방향과 국면별 분기.
- 화면을 고치면 `src/smoke.test.tsx`가 여전히 통과하는지 본다 (다섯 탭 + 설정이 안 죽는지).
- `npm run build && npm run lint && npm test` 세 개를 통과시키고 끝낸다.
- 앱 안 문구는 한국어. 운동 용어는 `RDL (루마니안 데드리프트)`처럼 병기.
- 설명은 코드를 모르는 사람 기준으로 쓴다. 파일명·함수명을 주어로 쓰지 않는다.

## 아직 없는 것

- 그래프 (허리둘레·체중 추세) — MVP2
- 러닝/클라이밍/불가리안백의 상세 기록은 숫자 4칸까지만
- 과훈련·일정 충돌 경고 (클라이밍 전날 등 고강도) — MVP3
- InBody, 사진 비교 — MVP3. 사진은 localStorage에 못 넣으므로 IndexedDB 필요

## 배포 — GitHub Pages

주소: https://yywoo734-source.github.io/training-log/

`main`에 push하면 `.github/workflows/deploy.yml`이 깃허브 러너에서 빌드해 배포한다.
**로컬 `dist`를 올리지 않는다** — 빌드를 깜빡한 채 옛 화면이 배포되는 사고를 막기 위함.

### `base`가 환경변수인 이유 (건드리기 전에 읽을 것)

Pages 주소가 루트가 아니라 `/training-log/` 하위경로다. `base: './'` 상대경로만으로는
`manifest.webmanifest`의 `start_url`/`scope`와 `registerSW.js`의 SW 등록 경로가 루트로 잡혀
**홈화면 추가와 오프라인 캐시가 조용히 죽는다** (화면은 멀쩡히 뜨므로 눈치채기 어렵다).
그래서 `base: process.env.BASE ?? './'`로 두고 워크플로에서 `BASE=/training-log/`를 넘긴다.

- 로컬 dev/preview·`npm run page`(단일 HTML)는 BASE 없이 그대로 상대경로로 동작한다.
- 저장소 이름을 바꾸면 워크플로의 `BASE`도 같이 바꿔야 한다.
- 확인법: 배포 후 `curl .../manifest.webmanifest`의 `scope`가 `/training-log/`인지 본다.

### 주소가 바뀌면 기록은 따라오지 않는다

localStorage는 출처(origin)별로 분리된다. localhost에서 쓰던 기록은 Pages 주소로 안 넘어온다.
설정 화면의 `백업 복사하기` → 새 주소에서 `붙여넣은 것 불러오기`로 옮긴다 (`store.ts`의 `exportJSON`/`importJSON`).
